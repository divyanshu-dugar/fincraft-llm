  services:
    - type: web
      name: fincraft-llm
      env: python
      buildCommand: "pip install -r requirements.txt"
      startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
      plan: free
      region: oregon
      envVars:
        - key: PYTHON_VERSION
          value: 3.11.9
